# IMPORTANT: placeholder values only — never commit real credentials.
# The DATABASE_URL in payment-gateway-secrets must match these credentials:
#   postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/payments?sslmode=disable
#
# To encode:  echo -n "your-value" | base64
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: default
  labels:
    app: postgres
type: Opaque
data:
  # echo -n "pguser" | base64
  POSTGRES_USER: cGd1c2Vy
  # echo -n "pgpassword" | base64
  POSTGRES_PASSWORD: cGdwYXNzd29yZA==
